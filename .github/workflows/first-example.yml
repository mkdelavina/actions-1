name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My first Github Action job"

        - name: run pwd
          run: pwd

        - name: read file
          run: cat README.md



